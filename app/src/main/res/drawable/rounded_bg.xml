<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:right="2dp">
        <shape>
            <solid android:color="@android:color/transparent" /> <!-- Optional: Use a solid color or remove this line for a transparent background -->
            <stroke
                android:width="0.5dp"
                android:color="@color/purple_500" /> <!-- Adjust the width and color of the border as per your requirements -->
            <corners android:radius="0dp"/>
        </shape>
    </item>
</layer-list>
